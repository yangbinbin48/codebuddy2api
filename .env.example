# ===============================================================
# CodeBuddy2API - 环境变量示例文件
#
# 使用前，请将此文件复制为 .env 并填写必要的值
# ===============================================================

# -----------------
# 服务端配置
# -----------------

# (必需) API服务的访问密码，客户端连接时需要在Authorization头中提供此密码
CODEBUDDY_PASSWORD=

# (可选) 服务监听的主机地址
# 默认值: 127.0.0.1
CODEBUDDY_HOST=127.0.0.1

# (可选) 服务监听的端口
# 默认值: 8001
CODEBUDDY_PORT=8001


# -----------------
# CodeBuddy API 配置
# -----------------

# (可选) CodeBuddy官方API的端点地址
# 一般无需修改
CODEBUDDY_API_ENDPOINT=https://www.codebuddy.ai

# (可选) 存放CodeBuddy认证凭证 (JSON文件) 的目录
# 默认值: .codebuddy_creds
CODEBUDDY_CREDS_DIR=.codebuddy_creds


# -----------------
# 高级配置
# -----------------

# (可选) 应用的日志级别
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
# 默认值: INFO
CODEBUDDY_LOG_LEVEL=INFO

# (可选) 向客户端报告可用的模型列表，用逗号分隔
# 用户可以根据自己的CodeBuddy账号支持的模型进行修改
# 注意: Anthropic 兼容端点会将 Claude 模型名(如 claude-sonnet-4-5)自动映射到此列表的第一个模型
CODEBUDDY_MODELS=claude-4.0,claude-3.7,gpt-5,gpt-5-mini,gpt-5-nano,o4-mini,gemini-2.5-flash,gemini-2.5-pro,auto-chat


# -----------------
# Anthropic 兼容 API 配置
# -----------------

# Anthropic Messages API 端点: POST /anthropic/v1/messages
# 用于 Claude Code 等工具直接连接，配置示例:
#
#   ANTHROPIC_API_KEY=<与 CODEBUDDY_PASSWORD 相同>
#   ANTHROPIC_BASE_URL=http://127.0.0.1:8001/anthropic
#   ANTHROPIC_MODEL=<CODEBUDDY_MODELS 中的一个模型>
#
# 认证方式: x-api-key header (值与 CODEBUDDY_PASSWORD 相同)
# 模型映射: Claude Code 发送的 claude-xxx 模型名会自动替换为 CODEBUDDY_MODELS 列表中的第一个可用模型